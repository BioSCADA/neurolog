<UserControl x:Class="BioSCADA.SessionPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Background="{DynamicResource BackgroundHighlighted}" FontFamily="Roboto" Height="306" Width="278">
    <Grid Margin="0"> 
        <DataGrid x:Name="dg_session" Margin="0,0,0,40" IsSynchronizedWithCurrentItem="True" Background="{DynamicResource BackgroundHighlighted}" Foreground="{DynamicResource Foreground}"   />
        <Button x:Name="bt_save" Content="Abrir..." Click="bt_save_Click" Margin="0,0,22,5" HorizontalAlignment="Right" Width="60" Height="30" VerticalAlignment="Bottom" Cursor="Hand"  />
    </Grid>
</UserControl>
